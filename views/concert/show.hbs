
<div class="container">
    <header class="hero is-dark ">
        <div class="hero-body">
            <h1 class="title has-text-centered">{{tour.name}}</h1>
        </div>
    </header>
    <main>
        <div>
            <div>
                {{#each bands}}
                    <ul>
                        <li>
                            <a href ="/{{_id}}">
                                {{name}} 
                            </a>
                        </li>
                    </ul>
                {{/each}}

                </br>
                </br>
                <a href ="/{{tour._id}}/new">Add Band</a>

                <a href ="/">Home</a>
            </div>
        </div>
    </main>
</div>